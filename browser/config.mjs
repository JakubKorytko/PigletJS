/** @import {Config} from "@jsdocs/browser/config.d" */

/** @type {Config} */
const config = {
  allowDebugging: true,

  enableCoreLogs: {
    info: false,
    warn: true,
    error: true,
  },
};

export default config;
